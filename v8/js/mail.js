function fetch_mail_info(player) {
	print('fetch_mail_info, player role_name:', player.role_name());
}

function pickup_mail(player, buf) {
	print('pickup_mail, player role_name:', player.role_name());
}

function delete_mail(player, buf) {
	print('delete_mail, player role_name:', player.role_name());
}

function send_mail(player, buf) {
	print('send_mail, player role_name:', player.role_name());
}